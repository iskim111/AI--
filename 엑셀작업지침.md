# 엑셀 작업지침

## 목적
이 문서는 Excel 활성 파일 작업, 시트 수정, 열 단위 처리, 대량 데이터 출력 작업을 오류 없이 빠르게 처리하기 위한 전용 지침이다.

## 최우선 원칙
1. 판단이 애매하면 사용자에게 질문해서 확인을 받는다.
2. 혼자 추측해서 다른 파일이나 다른 시트에 적용하지 않는다.
3. 엑셀 작업은 속도보다 정확한 대상 확인이 먼저고, 대상 확인 후에는 가장 빠른 방식으로 처리한다.

## 기본 원칙
1. 사용자가 엑셀에서 실행하라고 하면 먼저 현재 열려 있는 Excel의 활성 워크북과 활성 시트를 기준으로 작업한다.
2. 활성 파일에 바로 적용 가능한 작업인데 폴더의 엑셀 파일을 먼저 뒤지거나 추측해서 다른 파일에 적용하지 않는다.
3. 사용자가 특정 열과 출력 위치를 말했으면 그 열과 그 출력 열만 처리하고, 다른 시트나 다른 파일로 범위를 넓히지 않는다.
4. 실행 전에는 "활성 Excel 파일의 활성 시트에서 무엇을 어느 열에 넣는지"만 짧게 말하고 바로 실행한다.
5. 새 시트를 추가해야 하는 작업이면 기본적으로 기존 마지막 시트의 오른쪽에 생성한다.

## 속도 원칙
1. 엑셀 결과 출력은 가능한 한 셀 하나씩 반복 입력하지 말고 배열로 모아서 한 번에 출력한다.
2. 많은 행을 처리할 때는 반복문 안에서 셀 단위 쓰기를 최소화하고, 결과 배열을 만들어 대상 범위에 한 번에 넣는다.
3. 읽기도 가능하면 범위 단위로 한 번에 읽고, 메모리에서 처리한 뒤 결과만 범위 단위로 쓴다.
4. 단순한 엑셀 작업에서 속도를 느리게 만드는 주요 원인은 셀 단위 반복 쓰기, 파일 탐색, 방식 변경이다.

## 고정 실행 패턴
1. 이런 작업은 어렵지 않은 작업일수록 즉석에서 방식을 바꾸지 말고, 검증된 한 가지 패턴으로만 처리한다.
2. 기본 흐름은 `활성 Excel 연결 -> 활성 시트 확인 -> 입력 범위 한 번에 읽기 -> 메모리에서 처리 -> 출력 범위 한 번에 쓰기 -> 샘플 확인 후 종료`로 고정한다.
3. 실행 전에 확인할 것은 `활성 워크북 이름`, `활성 시트 이름`, `입력 열의 마지막 행` 정도로 최소화한다.
4. 실행 후에는 결과 열의 샘플 몇 개만 확인하고, 값이 정상적으로 들어갔으면 추가 탐색이나 다른 방식 시도를 하지 않는다.

## 권장 방식
1. 현재 열려 있는 Excel을 직접 다뤄야 하면 `COM 기반 활성 Excel 제어`를 우선한다.
2. 단순 파일 가공이나 새 결과 파일 저장이 목적이면 `pandas` 같은 파일 기반 처리를 쓴다.
3. 사용자가 보고 있는 현재 활성 시트에 바로 써야 하는 작업은 파일 기반 처리보다 활성 Excel 제어가 우선이다.

## 금지 목록
1. 활성 Excel 작업인데 폴더의 다른 `.xlsx` 파일부터 뒤지는 것
2. 사용자가 말하지 않은 다른 시트, 다른 파일, 다른 열까지 함께 수정하는 것
3. 많은 행을 처리하면서 셀마다 반복해서 쓰는 것
4. 실행 중간에 처리 방식을 바꾸는 것
5. 결과가 이미 들어갔는데도 계속 추가 실행이나 우회 시도를 하는 것

## 작업 체크리스트
1. 지금 작업 대상이 활성 워크북과 활성 시트가 맞는가?
2. 사용자가 지정한 입력 열과 출력 열만 처리하는가?
3. 입력은 범위 단위로 읽고 출력은 배열 일괄 출력으로 처리하는가?
4. 검증된 고정 패턴으로 실행하는가?
5. 결과 샘플 확인 후 바로 종료하는가?
6. 새 시트가 필요하면 오른쪽에 생성되도록 처리했는가?

## 한 줄 원칙
엑셀 작업은 활성 파일 기준으로, 범위는 한 번에 읽고 한 번에 쓰며, 확인되면 바로 끝낸다.
