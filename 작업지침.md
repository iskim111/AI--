# 코덱스 실수줄이기

## 목적
이 문서는 화면 문구 수정, 한글 데이터 수정, 작은 UI 변경 작업에서 코덱스가 같은 실수를 반복하지 않도록 하기 위한 작업 가이드다.

## 최우선 원칙
1. 판단이 애매하면 사용자에게 질문해서 확인을 받는다.
2. 혼자 추측해서 판단하지 않는다.

## 이번에 있었던 주요 실수
1. 작은 수정인데 공통 렌더링 함수까지 건드려 수정 범위를 불필요하게 키웠다.
2. 사용자가 요청한 위치를 정확히 확정하지 않은 채 추측으로 옮겼다.
3. 한글이 많은 데이터 파일을 넓게 재작성해 인코딩 깨짐을 유발했다.
4. 터미널에서 깨져 보이는 출력과 실제 파일 내용을 혼동했다.
5. 임시 보정 코드를 추가해 문제를 더 복잡하게 만들었다.
6. 수정 후 즉시 결과 확인보다 추가 수정을 먼저 해서 문제를 키웠다.
7. 사용자가 시킨 것보다 더 많은 변경을 했다.

## 반드시 지켜야 할 가이드

### 1. 작은 요청은 작은 수정만 한다
1. 문구 하나 수정, 간격 조정, 위치 이동은 해당 섹션 또는 해당 CSS만 수정한다.
2. 공통 렌더링 함수 수정은 같은 문제가 여러 곳에 동시에 있을 때만 한다.
3. 사용자가 특정 부분만 지목했으면 그 부분 외 영역은 건드리지 않는다.

### 2. 위치 요청은 추측하지 않는다
1. 사용자가 스크린샷이나 화살표로 지정한 위치를 최우선 기준으로 삼는다.
2. `위`, `아래`, `조금만`, `딱 여기` 같은 표현은 해석하지 말고 화면 위치 그대로 반영한다.
3. 위치 수정 후에는 “무엇을 어디에서 어디로 바꿨는지” 짧게 설명한다.

### 3. 한글이 많은 파일은 보수적으로 다룬다
1. 한글 데이터가 많은 JS/JSON/HTML 파일은 넓은 재작성보다 최소 패치를 우선한다.
2. 터미널 출력이 깨져 보여도 그것만 믿고 수정하지 않는다.
3. 한글 블록을 통째로 재작성해야 하면 먼저 안전한 방식인지 확인한다.
4. 인코딩 위험이 있으면 작은 범위 수정으로 쪼개서 반영한다.

### 4. UI 수정 순서
1. 먼저 데이터인지 스타일인지 렌더링인지 원인을 구분한다.
2. 데이터 문제면 데이터만 수정한다.
3. 간격 문제면 CSS만 수정한다.
4. 위치 문제면 margin, padding, display 구조를 확인한 뒤 한 항목만 수정한다.
5. 구조 변경은 마지막 수단으로만 사용한다.

### 5. 임시 우회 금지
1. DOM 조작으로 나중에 숨기거나 옮기는 임시 보정 코드를 기본 해결책으로 쓰지 않는다.
2. 화면이 이상할 때는 원인 파일을 고치지 않고 임시 스크립트로 덮지 않는다.
3. 우회 코드가 들어가면 이후 작업이 꼬이므로 가능한 한 넣지 않는다.

### 6. 수정 후 검증 순서
1. 문법이 깨지지 않았는지 먼저 확인한다.
2. 사용자가 요청한 영역만 바뀌었는지 확인한다.
3. 그 다음에 화면 결과를 확인한다.
4. 이상하면 새 수정부터 하지 말고 직전 수정이 맞았는지 먼저 되짚는다.

### 6-1. 구조나 스크립트 변경은 즉시 반영되게 한다
1. HTML에서 불러오는 JS/CSS 구조를 바꿨으면 브라우저가 새 파일을 읽도록 캐시 영향 여부를 바로 확인한다.
2. 정적 파일이 버전 쿼리(`?v=`)를 쓰는 구조면 수정 후 필요한 파일의 버전을 함께 올려 즉시 반영되게 한다.
3. "파일은 바뀌었는데 화면은 그대로"인 경우 내용 재수정보다 먼저 캐시와 로딩 파일 경로를 확인한다.
4. JS/HTML 안의 긴 문자열 내부에서 한 줄만 수정하거나 삭제할 때는 큰 블록 패치보다 정확한 문자열 치환을 먼저 쓴다.
5. 긴 문자열 한 줄 수정은 `대상 문자열 확인 -> 정확 문자열 Replace 1회 -> 버전 갱신` 순서로 끝내고, 큰 문맥 패치로 시간을 쓰지 않는다.

### 6-2. 엑셀 작업은 별도 지침을 먼저 본다
1. 엑셀 실행 요청, 활성 파일 수정, 시트/열 단위 처리, 대량 행 출력 작업은 먼저 [엑셀작업지침.md](d:/pys/AI_교육/AI 활용교육/엑셀작업지침.md)를 기준으로 처리한다.
2. 엑셀 작업에서는 일반 작업 습관보다 엑셀 전용 지침을 우선 적용해 오류를 줄이고 속도를 확보한다.
3. 특히 `활성 워크북/활성 시트 기준`, `배열 일괄 출력`, `검증된 고정 패턴`, `샘플 확인 후 종료` 원칙은 엑셀 작업에서 반드시 지킨다.

### 7. 원복 원칙
1. 방금 내가 만든 변경은 빠르게 원복할 수 있어야 한다.
2. 원복 요청이 오면 설명보다 먼저 해당 변경만 되돌린다.
3. 원복 시 다른 사용자의 변경이나 기존 변경은 건드리지 않는다.

### 8. 사용자 커뮤니케이션 원칙
1. 사용자가 화난 상태일수록 길게 변명하지 않는다.
2. 무엇을 수정하는지 먼저 말하고, 수정 후 무엇을 바꿨는지 짧게 보고한다.
3. 요청하지 않은 개선, 구조 변경, 리팩터링은 하지 않는다.
4. 결과가 불확실하면 추측하지 말고 확인 후 말한다.

## 작업 체크리스트
1. 이 수정이 정말 이 파일만 건드리면 되는가?
2. 공통 함수까지 건드릴 필요가 있는가?
3. 사용자가 말한 위치를 정확히 이해했는가?
4. 한글 인코딩 위험이 있는가?
5. 수정 범위를 더 줄일 수 있는가?
6. 지금 넣으려는 코드는 임시 우회가 아닌가?
7. 문제 생기면 바로 원복 가능한가?
8. 정적 JS/CSS/HTML 변경이 캐시 없이 바로 반영되게 했는가?
9. 엑셀 작업이면 먼저 [엑셀작업지침.md](d:/pys/AI_교육/AI 활용교육/엑셀작업지침.md)를 기준으로 잡았는가?
10. 긴 문자열 내부 한 줄 수정은 정확 문자열 치환으로 빠르게 처리했는가?

## 금지 목록
1. 작은 UI 수정을 이유로 데이터 구조 전체 재작성
2. 터미널에 깨진 문자열을 보고 그대로 덮어쓰기
3. 사용자가 지목하지 않은 위치까지 같이 수정
4. 검증 전에 연속 수정
5. 임시 DOM 보정 코드 추가
6. 원인 확인 없이 공통 렌더러부터 수정

## 권장 작업 방식
1. 사용자 요청 위치 확인
2. 최소 파일 1개 확인
3. 최소 패치 적용
4. 문법 확인
5. 결과 보고

## 한 줄 원칙
사용자가 시킨 자리만, 가장 작은 수정으로, 되돌릴 수 있게 바꾼다.
